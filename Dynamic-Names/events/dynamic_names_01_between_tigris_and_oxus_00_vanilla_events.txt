### generated events for dynamic names

namespace = dynamic_names_01_between_tigris_and_oxus_00_vanilla

country_event = {
    id = dynamic_names_01_between_tigris_and_oxus_00_vanilla.0
    hidden = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { tag = BAL } country_event = { id = dynamic_names_01_between_tigris_and_oxus_00_vanilla.1 } }
        if = { limit = { tag = ILK } country_event = { id = dynamic_names_01_between_tigris_and_oxus_00_vanilla.2 } }
    }

    option = {
        name = dynamic_names_01_between_tigris_and_oxus_00_vanilla.0.a
    }
}

country_event = {
    id = dynamic_names_01_between_tigris_and_oxus_00_vanilla.1
    hidden = yes
    is_triggered_only = yes

    trigger = {
        tag = BAL
    }

    immediate = {
        if = { limit = { AND = { has_reform = steppe_horde has_country_flag = is_KALAT_flag } NOT = { government_rank = 2 } } override_country_name = BAL_KALAT_HORDE }
        if = { limit = { AND = { has_reform = steppe_horde has_country_flag = is_KALAT_flag } government_rank = 2 NOT = { government_rank = 3 } } override_country_name = BAL_KALAT_KHANATE }
        if = { limit = { AND = { has_reform = steppe_horde has_country_flag = is_KALAT_flag } government_rank = 3 } override_country_name = BAL_KALAT_KHAGANATE }
    }

    option = {
        name = dynamic_names_01_between_tigris_and_oxus_00_vanilla.1.a
    }
}

country_event = {
    id = dynamic_names_01_between_tigris_and_oxus_00_vanilla.2
    hidden = yes
    is_triggered_only = yes

    trigger = {
        tag = ILK
    }

    immediate = {
        if = { limit = { has_country_flag = ere_form_ilkhanate_flag NOT = { OR = { has_country_flag = is_revolutionary_turkey_flag has_country_flag = is_turkey_flag } } is_subject = no government = monarchy primary_culture = turkish } override_country_name = ILK_DEVLET_I_ILKHANID }
    }

    option = {
        name = dynamic_names_01_between_tigris_and_oxus_00_vanilla.2.a
    }
}
