country_decisions = {
	qkh_return_to_steppes = {
		major = yes
		potential = {
			tag = QKH
			NOT = {
				has_country_flag = qkh_returned_to_steppes
			}
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
		}
		allow = {
			is_at_war = no
			OR = {
				num_of_owned_provinces_with = {
					value = 10
					OR = {
						has_terrain = steppe
						has_terrain = grasslands
						has_terrain = desert
					}
				}
				capital_scope = {
					OR = {
						region = mongolia_region
						region = central_asia_region
					}
				}
			}
		}
		effect = {
			set_country_flag = qkh_returned_to_steppes
			# Change government to tribal
			change_government = tribal
			add_government_reform = steppe_horde
			# Change religion to Tengri
			change_religion = tengri_pagan_reformed
			# Add modifier
			add_country_modifier = {
				name = "qkh_returned_to_steppes"
				duration = 9125
			}
			# Notify player
			custom_tooltip = qkh_return_to_steppes_tooltip
		}
		ai_will_do = {
			factor = 0
		}
	}
}
